import loginService from '../services/login'

const reducer = (state = [], action) => {
  switch (action.type) {
    case '':
      return
    case '':
      return
    default:
      return state
  }
}

export default reducer